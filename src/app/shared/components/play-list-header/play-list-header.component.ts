import { Component } from '@angular/core';

@Component({
  selector: 'app-play-list-header',  // Definición del selector que se utilizará en el HTML
  imports: [],  // Lista de módulos que este componente importa. Está vacío en este caso.
  templateUrl: './play-list-header.component.html',  // Ruta al archivo de plantilla HTML para el componente
  styleUrl: './play-list-header.component.scss'  // Ruta al archivo de estilo SCSS para el componente
})
export class PlayListHeaderComponent {
  // Esta clase representa el componente de la cabecera de la lista de reproducción.
  // Actualmente, no contiene lógica, pero puede ser extendida en el futuro con propiedades y métodos.
}
